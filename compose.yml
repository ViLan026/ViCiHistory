services:
  ollama:
    image: ollama/ollama:0.34.1
    restart: unless-stopped
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - history_ai

  api:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      OLLAMA_URL: http://ollama:11434
    depends_on:
      - ollama
    ports:
      - "8080:8080"
    networks:
      - history_ai

volumes:
  ollama_data:

networks:
  history_ai: